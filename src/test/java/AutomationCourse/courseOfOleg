004.
В java примитивы - это базовые типы данных, которые представляют числа и логические значения.Всего существует 8 примитивных типов:
byte, short, int, long, float, double, boolean, char.

Ссылочные типы данных в Java - это объекты, хранящиеся в куче (heap) и ссылки на них, которые хранятся в стеке (stack).
Ссылочные типы данных могут содержать как примитивные значения, так и другие ссылочные типы данных.
Примерами ссылочных типов данных в Java являются String, Object, ArrayList.

Отличия между примитивами и ссылочными типами данных заключаются в том, что примитивы имеют фиксированный размер и передаются по значению (не копируются),
тогда как объекты передаются по ссылке (адресу в памяти) и могут быть изменены не только внутри метода, но и за его пределами.

015. HashSet - убирает дублирование в списке.
HashMap - если ключ-значение дублируется, то используется только одно ключ-значение.
Если два ключа одинаковых, то используется последнее значение.